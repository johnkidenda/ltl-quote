Playable quote-and-book mock.

Flow: landing → mock login → shipment details → additional services → routes → addresses → customs (if destination is not US) → confirm and book.

Rates, carriers, and transit are mocked. Charles / X-freight is not wired. Book writes a CSV + browser log. No payments. Login accepts any non-empty username and password.
